echo "[husky] Running pre-push checks..."

npm run lint || exit 1
npm run docker:test:unit || exit 1

# Optional fast integration smoke (uncomment if needed)
# npm run test:integration -- -t "auth" || exit 1

echo "[husky] All checks passed. Proceeding with push."
